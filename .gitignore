__pycache__/
.venv/

# Private data — lives in the separate private repo, never in this public one.
# Listed here so a local copy for testing can never be committed by accident.
feeds.yaml
coverage.yaml
lens_prompt.txt
# Commercial strategy & personal notes — keep private, not for the public repo.
PRODUCT_ROADMAP.md
ENGINEERING_NOTES.md
TECHNICAL.md
# Owner-only utilities (historical export / verification) — not a public feature yet.
private-tools/
history-wide.csv
homepage_layout_preview.html
first_build_preview.html
restructure_preview.html
# Root-only: the private cache/history dir. Anchored with a leading slash so it does
# NOT match docs/data/ (the public CSV/JSON export, which must be committed).
/data/
*.env
.env
layout_preview.html
coverage-pipeline/
feed_tab_preview.html
trends_tab_preview.html
sources_tab_preview.html
header_preview.html
