__pycache__/
.venv/

# Private data & internal notes — pulled from the private repo or kept local; never public.
feeds.yaml
tech_registry.yaml
coverage.yaml
PRODUCT_ROADMAP.md
ENGINEERING_NOTES.md
TECHNICAL.md
ANALYTICS_SPEC.md
FILE_INVENTORY.md
SCHEMA.md
SOURCE_AUDIT.md
CURATION_PROTOCOL.md
history-wide.csv

# Root-only: the private cache/history dir. Anchored with a leading slash so it does
# NOT match docs/data/ (the public CSV/JSON export, which must be committed).
/data/
*.env
.env

# Local design mock-ups / scratch snapshots — never part of the build.
*_preview.html

# Internal evaluation harness & known-event sets — private.
evals/
